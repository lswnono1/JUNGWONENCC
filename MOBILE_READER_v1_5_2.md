# 모바일 원문 열람 수정 v1.5.2

작성일: 2026-09-07
운영 앱: https://lswnono1.github.io/JUNGWONENCC/lawmonitor/

## 수정 방식

법제처의 기존 모바일 HTML API를 실제 안드로이드 브라우저 조건으로 실행했을 때 빈 화면 또는 PC 화면으로 이동하는 현상을 확인했다. 단순히 주소에 mobileYn=Y를 추가하는 것으로는 해결되지 않았다.

이번 버전은 외부 법제처 모바일 사이트 자체를 수정한 것이 아니다. 국가법령정보센터의 법령·행정규칙 본문 JSON API에서 받은 내용을 앱 안의 반응형 모바일 열람창으로 표시한다. 공식 원문 텍스트를 요약하거나 임의로 수정하지 않는다.

본문, 부칙, 별표·서식, 글자 크기 조절, 닫기 기능을 제공한다. API에 PDF 또는 원본 파일 링크가 있으면 해당 공식 파일을 열 수 있다. 표의 열·병합 형식은 PDF/원본 파일을 기준으로 확인한다.

## 기존 자료와 API 설정

기존 core 1~8, IndexedDB 구조와 저장자료를 유지했다. 데이터 초기화, API OC 재발급 또는 앱 삭제는 이번 업데이트의 설치 절차가 아니다. 현재 앱에 입력한 OC로 요청하며, 시험용 OC를 사용자용 프로그램에 대체값으로 넣지 않았다. 계정별 본문 API 이용권한은 별도로 충족되어야 한다.

기존 PC 주소와 기존 모바일 HTML 주소도 앱 열람창으로 처리한다. 링크에는 선택한 법령 이력 번호와 공포일을 보존하며, 공포일·식별번호가 API 응답과 다르면 다른 법령으로 바꾸지 않고 오류를 표시한다. 공포일 기준 API 본문과 목록의 시행일이 다르면 단계 시행 여부를 확인하도록 경고한다.

## 배포 전 검증

검증 소스: repair branch commit 922b62fb81315f6d60f1e4ae44ebfeab0a9c7420
검증 실행: https://github.com/lswnono1/JUNGWONENCC/actions/runs/34083423973

41개 단위 테스트가 통과했다. 모바일 Chromium의 Pixel 7 화면 조건에서 공식 시험용 OC를 이용해 다음 원문을 실제 조회·표시했다.

- 자동차관리법: 본문 표시, 외부 팝업 없음.
- 공식 행정규칙 예제: 본문 및 서식 원본 링크 표시.
- 소방시설 설치 및 관리에 관한 법률 시행령: 본문, 별표 목록, 공식 PDF/원본 링크 표시.
- 옥내소화전설비의 화재안전성능기준(NFPC 102): 본문 표시.

연속 문서 열기, PC 주소 처리, OC 미입력 오류 안내, 실제 앱 로더 시작, 모바일 화면 가로 넘침을 함께 검사했다. 운영 반영 후 검증 워크플로에서는 한글 글꼴이 설치된 시험 환경의 화면 기록과 배포 파일 일치 결과를 저장한다.

## 사용 시 확인

앱을 완전히 닫았다 다시 열거나 Chrome에서 운영 주소를 새로고침한다. 관리 또는 설정 화면에 '모바일 원문 v1.5.2'가 표시되는지 확인한 뒤 원문 버튼을 누른다. 앱/사이트 데이터를 삭제하지 않는다.

## 확인 범위의 한계

시험용 공식 OC 및 브라우저의 모바일 화면 에뮬레이션으로 검증했으며, 실제 사용자 휴대폰·사용자 OC·모든 법령 이력의 동작을 보장하는 것은 아니다. PDF/HWP 파일의 열람 방식은 휴대폰 브라우저와 설치된 뷰어에 따라 달라질 수 있다. API 응답에 없는 내용은 임의로 보충하지 않는다.
