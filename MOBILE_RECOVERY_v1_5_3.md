# 모바일 연결 복구 v1.5.3

2026-09-07. 사용자 보고: 기본정보 조회 실패.

v1.5.2 열람창의 자체 오류 문구에는 해당 문구가 없고, 이전 실측에서 법제처 외부 lsInfoR 응답의 alert로 확인된 문구이다. 사용자 휴대폰의 화면·주소·실행버전을 받지 않았으므로 그 휴대폰의 단일 원인을 확정한 것은 아니다.

## 수정

기존 core 1~9를 보존하고 core 10을 추가한다. 일반 진입화면과 새로운 recover-v1.5.3.html은 같은 버전의 통합 실행 파일 app-v1.5.3.js를 무결성 값과 함께 읽는다. 구형 app.js 진입주소도 같은 실행본으로 연결한다. 화면에 실행 중인 버전을 표시한다.

기존 원문 버튼뿐 아니라 해당 법령의 일반 하이퍼링크도 앱 내부 열람창으로 처리한다. 공식 API가 기본정보 조회 실패 HTML을 반환하면 그 스크립트를 실행하지 않는다. 법령명·공포일과 기존 법령 ID가 일치하는 이력이 유일한 경우에만 새로 확인된 이력 번호로 한 번 재요청한다. 불일치·복수 후보는 현재 법령으로 대체하지 않는다. 실패 시 인증값을 제외한 연결 진단을 표시한다.

IndexedDB 구조·관리대상·개정이력·API 설정을 삭제·초기화하지 않는다. 다른 앱의 저장소나 캐시는 삭제하지 않는다. 본문 API는 기존 사용자 OC로 요청하며 시험용 OC를 대체값으로 넣지 않는다.

## 확인 방법

Chrome에서 기존 홈 화면 아이콘 대신 https://lswnono1.github.io/JUNGWONENCC/lawmonitor/recover-v1.5.3.html 을 한 번 연다. 화면의 모바일 원문 v1.5.3 표시를 확인한다. 같은 Chrome 프로필에서는 기존 자료를 사용한다. 초기 설정 화면이 뜨면 다른 브라우저·프로필·구형 APK일 수 있으므로 새로 설정하지 말고 화면을 확인한다.

## 검증 한계

오류 재현 및 자료 보존 시험은 별도의 가상 계정·시험 데이터로 수행한다. 실제 원문 연결 시험은 법제처 공식 예제 OC와 모바일 화면 에뮬레이션을 사용한다. 사용자 기기의 오류 원인이나 실제 계정의 API 이용권한까지 확인한 것은 아니다.
